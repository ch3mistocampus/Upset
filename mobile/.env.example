# ===========================================
# Upset App - Environment Variables
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# -------------------------------------------
# Supabase Configuration (Required)
# -------------------------------------------
# Get these from: https://supabase.com/dashboard/project/_/settings/api
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# -------------------------------------------
# Google OAuth (Optional - for Google Sign-In)
# -------------------------------------------
# Create credentials at: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client IDs for Web, iOS, and Android
# 2. Add authorized redirect URIs in Google Console
# 3. Add Web Client ID to Supabase Auth providers
EXPO_PUBLIC_GOOGLE_CLIENT_ID_WEB=your-web-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_CLIENT_ID_IOS=your-ios-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_CLIENT_ID_ANDROID=your-android-client-id.apps.googleusercontent.com

# -------------------------------------------
# Sentry Error Tracking (Optional in dev)
# -------------------------------------------
# Get your DSN from: https://sentry.io/settings/[org]/projects/[project]/keys/
# Errors only sent in production builds
EXPO_PUBLIC_SENTRY_DSN=https://your-key@your-org.ingest.sentry.io/project-id

# -------------------------------------------
# App Environment
# -------------------------------------------
# Set automatically by EAS Build, manually for local dev
EXPO_PUBLIC_APP_ENV=development

# ===========================================
# SECURITY NOTES
# ===========================================
# NEVER add these to client code - use Edge Functions instead:
# - SUPABASE_SERVICE_ROLE_KEY (bypasses RLS - server-side only)
# - Any external API keys (use Edge Functions as proxy)
#
# For production builds, use EAS Secrets:
# eas env:create --name EXPO_PUBLIC_SENTRY_DSN --value "..." --environment production --visibility sensitive
